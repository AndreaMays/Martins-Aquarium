export const Tip = (tips) => {
    return `
        <section class="containerRight ">
            <div class="travel">${tips.words}</div>
            <div class="travel">${tips.words}</div>
        </section>
    `
}